<div class="container">
	<h1 class="text-center"><%= @store.name %></h1>
		<div class="row">
			<% @products.each do |product| %>
				<div class="col-md-4"><img src="<%= product.image_url %>" class="img-responsive"></div>
			<% end %>
		</div>
</div>